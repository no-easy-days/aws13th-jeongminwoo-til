# 3.1. 자료형 실습 문제

## 🔥 실습 3-1: 이메일 주소 분리하기
```python
// jmw010314@naver.com
email = input("이메일 주소를 입력하세요: ")
parts = email.split("@")
username = parts[0]
domain = parts[1]

print(f"사용자 이름: {username}")
print(f"도메인: {domain}")
```
## 🔥 실습 3-2: 비밀번호 길이 검사
```python
password = input("비밀번호를 입력하세요: ")

if len(password) >= 8:
    print("✅ 유효한 비밀번호입니다.")
else:
    print("비밀번호가 8자 미만입니다")

```
## 🔥 실습 3-3: 3의 배수 찾기
```python
a = []

for i in range(1,21):
    if i % 3 == 0:
        a.append(i)

print(a)
```
## 🔥 실습 3-4: 공통 관심사 찾기
```python
chulsoo = ["축구", "영화", "음악", "게임", "독서"]
younghee = ["영화", "음악", "요리", "여행", "독서"]

result = set(chulsoo) & set(younghee)
print(result)
```
## 🔥 실습 3-5: 최고 점수 학생 찾기
```python
scores = {
    "철수": 85,
    "영희": 92,
    "민수": 78,
    "지수": 95,
    "현우": 88
}

top_student = max(scores, key=scores.get)
print(f"최고 점수 학생: {top_student}, 점수 : {scores[top_student]}") 
```
